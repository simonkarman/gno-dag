import { PlayerName } from '@/components/puzzles';

/** Per-player marker colors used across the game and admin UI. */
export const PLAYER_COLORS: Record<PlayerName, string> = {
  'Govie': '#f59e0b', // amber
  'Jac.': '#3b82f6',  // blue
};
